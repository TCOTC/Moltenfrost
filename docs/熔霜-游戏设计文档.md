# 熔霜 · Moltenfrost 游戏设计文档

> 项目定位：借鉴经典双人元素解谜玩法内核，通过 PVE 系统拓展与多设备联机协作，打造一款**独立原创**的解谜动作游戏。
> 日期：2026-09-23
> 修订：2026-09-23 —— 确定为**仅桌面端**发行，支持 **Windows 与 macOS**（开发用的两台笔记本即这两个系统），各机器只导自己平台的产物；macOS 产物为 Universal 2，但**只支持 Apple Silicon**，Intel Mac 不在支持范围；双人协作为**多台设备联机**，不做单机同屏
> 修订：2026-09-25 —— **放弃 3D，改为 2D 横版**。3.2 的立体关卡与视角切换随之作废，4.3 移除 3D 相机插件，理由见 3.0。已完成的联机骨架与远端位置平滑与维度无关，只改向量类型即可复用。
> 命名、法律边界与合规调研见 `命名与合规调研.md`

---

## 一、项目概述

### 1.1 玩法内核参考

经典网页双人解谜系列（英文正名 *Fireboy and Watergirl*，中文俗称《森林冰火人》）由独立开发者 Oslo Albet 于 2009 年发布，核心玩法为：

- 双人/单人双控协同闯关
- 两名角色具备相克元素属性（火怕水、水怕岩浆）
- 收集宝石、触发机关、规避属性陷阱
- 纯解谜、无战斗、无成长线

### 1.2 项目目标

在**保留"双元素协同解谜"这一抽象玩法内核**的前提下，完成三件事：

1. **2D 横版重构** —— 保留平面卷轴的形态，把工程投入集中在手工关卡与双人协同机制上（2026-09-25 决定，理由见 3.0）
2. **PVE 拓展** —— 加入战斗、怪物、成长系统，实现长线运营
3. **彻底原创化** —— 名称、美术、关卡、数值、代码全部自主创作（合规要求见 `命名与合规调研.md`）

---

## 二、命名

### 2.1 游戏名：熔霜 / Moltenfrost

| 项 | 内容 |
|---|---|
| 中文名 | **熔霜** |
| 英文名 | **Moltenfrost** |
| 意象 | 熔（熔岩·火）+ 霜（寒霜·水），两字均为"临界状态"——熔是固液之间，霜是气固之间 |
| 优势 | 两字极简、读音干脆；不含"冰/火"任何一字，联想风险低；合成词显著性高于 `A & B` 词组结构 |

读音：Moltenfrost 为自造合成词（Molten + frost），重音落在第一音节，英 /ˈməʊltənfrɒst/、美 /ˈmoʊltənfrɔːst/。

### 2.2 角色名：熔 & 霜

| 属性 | 中文 | 英文 | 备选拼音 |
|---|---|---|---|
| 火 | **熔** | Molten | Rong |
| 水 | **霜** | Frost | Shuang |

**设计要点**

- 与游戏名《熔霜》同源，品牌高度统一
- 「熔」「霜」均为状态词而非元素直呼，辨识度高于直呼型命名，且完全原创
- 英文名 Molten / Frost 与游戏名 Moltenfrost 呼应；若担心与游戏名过度重叠，可改用拼音 **Rong & Shuang**，进一步降低商标关联风险
- 造型建议：熔——熔岩结晶体，裂纹中透出橙红流光；霜——冰霜能量体，表面凝霜、边缘折射冷光

---

## 三、2D + PVE 差异化设计方向

### 3.0 为什么不做 3D（2026-09-25 决定）

原定方向是“从 2D 平面卷轴升级为三维空间解谜”，现改为保留 2D 横版。四条理由：

- **2D 上成立的机制很难平移到 3D。** 双人解谜里最有效的约束是“两人各自只有一条路、必须按顺序错开”，而它建立在“一个平面内位置有限”之上。多出一个轴之后，玩家往往能绕过去，约束不再成立，剩下的只是移动变慢与镜头变难。
- **3D 的资产与关卡成本远高于 2D 手绘。** 一个 3D 房间需要建模、材质、光照与碰撞体四份工作，2D 一个房间是分层贴图加手画的多边形碰撞。原型阶段要反复试关，这个倍数会直接变成验证速度的差距。
- **3D 独有的收益一项都还没实现。** 立体关卡、视角切换、可旋转机关都只在文档里，因此这次改变没有沉没成本。
- **已完成的联机部分与维度无关。** 连接层、权限与生成同步、远端位置平滑三件事都不依赖维度，2D 化只改了向量类型。因此之前的工作没有白做。

代价要一并记下：与参考作品的距离在“维度”这一项上回到同一水平。原创性改由三件事承担——多设备联机协作、PVE 战斗与成长、多元素反应网络。这三件事必须在成品里真的做出来，合规论证的更新见 `命名与合规调研.md`。

### 3.1 元素反应系统（核心创新）

突破原版"只有两种元素"的设定，构建多元素反应网络：

| 组合 | 效果 |
|---|---|
| 火 + 风 | 火焰蔓延，点燃范围区域 |
| 水 + 雷 | 导电，连锁触发电气机关 |
| 火 + 水 | 蒸汽，产生上升气流或遮蔽视野 |
| 土 + 水 | 泥沼，减速或固化地形 |
| 雷 + 土 | 磁化，吸附金属物件 |

> 这是原版完全不具备的深度，也是玩法原创性的最强证明。

**熔 & 霜 专属反应**

| 组合 | 效果 |
|---|---|
| 熔 + 霜 | 骤冷成器，熔岩凝为可通行平台 |
| 熔 + 霜（水汽） | 蒸汽爆发，推动风车机关或升举重物 |

### 3.2 平面关卡设计

- **一屏房间**：关卡由若干手工房间串联，单个房间通常一屏看完。每位玩家一台跟随自己角色的相机，因此两人可能各自看到房间的不同部分，信息差就此成为协作的一部分。
- **分层纵深**：背景、可玩层、前景分三层，用视差滚动与相互遮挡表达深度。这是 2D 里表达纵深的主要手段，也是“看起来有层次”与“实际上是一个平面”之间唯一的差别。
- **手绘碰撞**：地面与平台用 `CollisionPolygon2D` 手画，碰撞形状与美术形状一一对应，不依赖网格吸附。
- **二维几何的机关**：升降平台、传送带、跷跷板、可推动的箱子。这些在平面里判定清楚、玩家一眼看懂；搬到 3D 反而会因为多出来的自由度失去约束（见 3.0）。

### 3.3 PVE 系统

- **元素克制战斗**：怪物携带元素护盾，仅对应属性角色可破除，强制双人协作
- **长线运营**：副本分普通 / 精英 / 深渊，配合技能树、装备与角色养成，并设多阶段元素机制 BOSS

### 3.4 单人体验优化

- **AI 同伴**：解决原版"必须两个人才能玩"的最大痛点
- **指令系统**：可对 AI 同伴下达"待命 / 跟随 / 触发机关"等指令
- **联机形态**：多台设备，见 3.5

### 3.5 联机形态

- **每个玩家各用一台设备**，双人协作通过联机实现；不做单机同屏双人，因此不需要处理两人共用一套键鼠、共用一台相机的问题
- **每位玩家拥有独立视角**：每位玩家一台跟随自己角色的相机（见 3.2），两人各自看到自己角色所在的那一屏，而不是共享一个画面。看到的范围不同，因此“描述自己那边的情况”成为主要操作
- **局域网优先**：同一网络内直连即可验证协作手感；公网联机作为后续拓展

---

## 四、技术选型

| 项 | 内容 |
|---|---|
| 目标平台 | **仅桌面端**：Windows（x86_64，Intel 与 AMD 通用）与 macOS（Universal 2 产物，支持范围只到 Apple Silicon）；各机器只导自己平台的产物，不做网页版与移动端 |
| 引擎 | **Godot 4**，项目唯一引擎，不引入 Unity 或 Unreal Engine |
| 脚本语言 | GDScript 为主；需要静态类型与 .NET 生态时改用 C#（使用 .NET 版编辑器） |
| 渲染后端 | Forward+（桌面默认），与编辑器预览一致，不需要按平台分流；2D 场景同样走它，不必改用兼容渲染器 |
| 物理 | 引擎内置 2D 物理，用于推箱、升降平台与浮力。2D 侧没有可选后端：`physics/2d/physics_engine` 只有 `GodotPhysics2D`（以及关闭物理的 `Dummy`），Jolt 只服务 3D |
| 分辨率与缩放 | 基准 1920×1080，画布项拉伸加分数缩放，纹理过滤保持线性。美术是高清手绘与矢量，因此不做像素对齐，也不用整数缩放 |
| 联机 | 引擎高层多人 API（`MultiplayerAPI`），传输层用 `ENetMultiplayerPeer`（UDP）主机/加入者模式，多台设备互联；不引入 Photon 或 Mirror |
| AI 同伴 | 有限状态机或行为树；引擎没有内置行为树，可用社区插件或自行实现 |

联机、物理与 AI 全部在引擎能力范围内实现，不为上述任何一项引入第三方引擎或中间件。

第三方库仅限于引擎插件层面，准入规则与清单见 4.2 与 4.3。

### 4.1 平台约束

本项目只做桌面端，且要同时覆盖 **Windows 与 macOS**——开发用的两台笔记本就是这两个系统，日常在两边来回切换。**各机器只导自己平台的产物**：Windows 机器只出 Windows 版，Mac 机器只出 macOS 版，跨系统导出带来的坑（可执行位、DMG、签名工具）因此全都不存在。Windows 侧支持 x86_64（Intel 与 AMD 通用）；macOS 侧产物是 Universal 2，但支持范围只到 Apple Silicon，Intel Mac 不在范围内。双人协作为多台设备联机，不做单机同屏（见 3.5）。发行走桌面商店，没有浏览器沙箱与单文件体积上限；输入用桌面原生输入，键鼠与手柄各维护一份完整映射，并支持运行时重绑定。

只做桌面端意味着**不适用**网页端的一整套限制：指针锁定与全屏不需要用户手势触发、音频没有自动播放策略、存档是真实文件系统而不是 IndexedDB、不需要 HTTPS 与 Secure Context。这些限制从选型依据中移除，也不再作为排除任何库的理由。

对第三方库的影响：不再要求"必须是纯 GDScript 插件"，GDExtension 可以正常使用。但仍**优先**选纯 GDScript 插件——它们不携带平台二进制，升级 Godot 时不需要等作者为两个平台重编。

两台机器带来的三类额外工作：

| 项 | 内容 |
|---|---|
| 输入键位 | macOS 的主修饰键是 Cmd（`KEY_META`），Windows 是 Ctrl；同一套快捷键在另一边会别扭，InputMap 需要按平台分支 |
| 高分屏 | macOS 的 Retina 与 Windows 的显示缩放都会改变实际渲染分辨率；描边宽度与 1 px 细线这类"按像素数"的美术要按设备像素比核对，不能只在一种屏上调好 |
| 工具链 | 自检与构建脚本必须跨平台（优先 Node 单实现，不写 PowerShell 专用逻辑），否则换到另一台笔记本就少掉一半验证能力 |

导出与签名两边并不对称，提前知道可以省一轮返工：

| 项 | Windows（在 Windows 机器上导） | macOS（在 Mac 机器上导） |
|---|---|---|
| 目标架构 | x86_64（Intel 与 AMD 通用，导出默认值） | Universal 2。官方模板里只有 `godot_macos_*.universal` 一个二进制，预设的 `binary_format/architecture` 选 `arm64` 或 `x86_64` 都会因找不到对应文件而导出失败 |
| 签名 | 可选，不签只有 SmartScreen 提示 | **发布给外部用户必须签名并公证**，否则被 Gatekeeper 拦截；需要 Apple Developer 账号。签名与公证只能在 macOS 上做，所以这条链整体留在 Mac |
| 产物流向 | 只产出 Windows 产物 | 只产出 macOS 产物 |

支持范围与产物架构是两件事：产物是 Universal 2，Intel Mac 能运行，但它不在支持范围内（2020 年之前的机型）——**测试与验收必须在 Apple Silicon 机器上做**。若要真正只出 arm64，需要在 Mac 上导出后用 `lipo -thin arm64` 削掉另一半，且必须削在签名之前；是否做这一步留待发布流程确定。

### 4.2 第三方库准入规则

| 许可 | 处置 |
|---|---|
| MIT / BSD / Apache-2.0 / Zlib / CC0 / Unlicense | 可用，保留版权声明与许可文本 |
| LGPL | 谨慎，动态链接的认定口径有争议 |
| GPL / AGPL | 禁用，传染性条款与闭源商业发行冲突 |
| CC-BY-NC* | 仅原型期可用，上线前必须替换 |

三条附加规则：

1. **代码许可与素材许可分开核查**。插件自带的字体、图标、音效、示例工程美术往往另有许可（例如 GDQuest 的 TPS Demo 代码是 MIT，美术却是 CC-BY-NC-SA）。
2. **登记台账**。每引入一个第三方库，在项目根目录 `THIRD_PARTY.md` 记录名称、版本、许可、来源链接与是否改动过，对应合规调研文档 2.3「资产」中的授权凭证留存要求。
3. **升级 Godot 后回归验证**。插件跟随引擎小版本失效是常态，升级引擎前先跑一遍自检。

### 4.3 已选定的第三方库

以下许可均为 MIT，可用于闭源商业发行。"类型"一列决定升级 Godot 时的维护成本：纯 GDScript 插件不携带平台二进制，最省事；GDExtension 需要作者提供对应平台的构建。

| 库 | 用途 | 类型 | 引入阶段 |
|---|---|---|---|
| `derkork/godot-statecharts` | 角色与机关的状态图（站立 / 移动 / 冻结 / 融化、机关开合） | 纯 GDScript | 第 2 阶段 |
| `Ark2000/PankuConsole` | 运行时调试面板，打包后的桌面版本里也能调参、看状态 | 纯 GDScript | 第 2 阶段 |
| `Maaack/Godot-Menus-Template` | 菜单、设置与输入重绑定界面 | 纯 GDScript | 第 2 阶段，按需 |
| `bitbrain/beehave` | AI 同伴的行为树（Godot 4 版本在 `godot-4.x` 分支） | 纯 GDScript | 第 3 阶段 |
| `OctoD/godot-gameplay-systems` | 属性、技能与能力系统，支撑技能树与装备成长 | 纯 GDScript | 第 3 阶段 |
| `foxssake/netfox` | 联机状态同步与延迟补偿，配合 ENet 使用 | 纯 GDScript | 第 3 阶段 |
| `foxssake/noray` | 公网联机的连接编排与中继（解决 NAT 穿透），netfox 的配套服务端 | 服务端（Node） | 暂不引入——4.5 已定为自建权威服务端，不再需要中继编排 |
| `shomykohai/quest-system` | 任务系统 | 纯 GDScript | 第 4 阶段 |
| `nathanhoad/godot_dialogue_manager` | 对话与叙事 | 纯 GDScript | 第 4 阶段，按需 |

原型阶段的瓶颈是手感验证而不是功能缺失，因此第 2 阶段只引入状态图、运行时调试面板与菜单模板，其余按阶段逐步加入。

### 4.4 备选与排除

改做桌面端之后，"GDExtension 在网页版跑不起来"这一整类排除理由已不成立，下表按新的实际情况重列。

| 项 | 处置 | 原因 |
|---|---|---|
| LimboAI | 备选，暂不引入 | 桌面端可正常使用，行为树功能强于 Beehave；先按 Beehave 起步，复杂度上来再评估替换 |
| Terrain3D、godot-orchestrator、godot-sqlite | 暂不引入 | 技术上可用，但本项目的关卡是手工房间，没有地形、可视化脚本与数据库需求 |
| 调试绘制插件（Debug Draw 等） | 按需引入 | 调试物理射线与碰撞形状时有用，只在开发期开启。2D 侧引擎自带的「可见碰撞形状」多数情况已经够用 |
| Photon | 排除 | 引擎自带 ENet 已满足联机需求，不引入第三方中继或服务器 SDK |
| GDQuest TPS Demo 等示例的美术资源 | 排除 | CC-BY-NC-SA，禁止商用 |
| 任何 GPL / AGPL 插件 | 排除 | 传染性条款与闭源商业发行冲突 |

### 4.5 由选型派生的两条实现约束

**2D 物理没有第二套后端可选。** 4.7.2 里 `physics/2d/physics_engine` 的可选值只有 `GodotPhysics2D` 与关闭物理的 `Dummy`，Jolt 只服务 3D。项目设置里仍然显式写了 `GodotPhysics2D`，目的是让“用的是哪一套”在配置里可见，而不是依赖 `DEFAULT` 当下的含义。代价是堆叠与旋转平台的求解稳定性弱于 Jolt，因此 B 类物理玩法（见 `玩法方案库.md`）上马前要先做原型验证，不能按 3D 版的预期排期。

**联机走 ENet，权威节点自建。** 两个系统都内置 `ENetMultiplayerPeer`（UDP），不需要额外依赖，局域网内直接连 IP 就能验证协作手感（macOS 首次监听端口会弹一次防火墙授权）。

公网形态在 2026-09-24 定为**自建权威服务端**，它与 Steam 数据报中继、EOS P2P 的分歧点只有一个：由谁解决可达性。本项目面向的玩家与两台开发机一样没有公网入口，因此只有“权威节点自带公网地址”这一条能从根上移除这个问题；Steam 侧官方明确写了非 Steam 渠道不保证服务，EOS 侧则是 Godot 绑定由社区维护。自建服务端的代价是一台 VPS 与月度费用，并发能力上限等于进程数。

服务端与客户端是同一个 Godot 工程，以 `--headless` 启动（可参考官方"Exporting for dedicated servers"，它可以在导出预设里选取专用服务端模式并预先剔掉客户端才需要的资源）。这么定不是因为省事，而是因为服务端要执行物理与机关判定：若另用一套实现，就得把 2D 刚体动力学、碰撞形状与接触求解重写一遍，而两套实现必然分叉，这个项目的核心玩法又恰好建立在物理之上。

玩家移动暂时由本机权威判定，推箱与升降平台则要在引入时改为服务端权威；是否把玩家移动一并改为“上报输入 + 服务端模拟”要等网络损伤测试的结果，那一步会引入客户端预测与位置回滚（netfox 一类方案）。

### 4.6 初始界面与局域网房间探测

2026-09-26 加入。启动流程据此调整：有画面且不带参数时停在初始界面，由玩家创建房间或加入房间；`--host` 跳过界面直接开局，`--join <地址>` 跳过界面直接加入；无头启动仍按专用服务端处理，因为没有人能点界面。界面上的“创建房间”与 `--host` 是同一段代码，因此自动检查覆盖的路径与玩家实际遇到的路径一致，不会出现“检查通过而玩法路径已经坏了”这一类分叉。对局中按 Esc 返回初始界面：客户端等于离开房间，主机等于关掉房间。

**房间列表由主机定期广播，而不是加入者广播询问。** 分歧点是端口占用：后者要求主机也绑定探测端口才能收到询问，于是同一台机器上“主机 + 界面”两个实例会同时使用同一个端口；前者让界面独占该端口、主机用临时端口发送，两边互不影响，代价是主机每秒多发出一个很小的包。探测端口固定 27016，与游戏端口（默认 27015）分开，因此主机换游戏端口不影响探测。

**广播发两份：受限广播 255.255.255.255 与 127.0.0.1。** 受限广播会被送到所有接口，因此在装有虚拟机网卡的机器上不需要指定网卡；而它会不会被发回本机由系统决定，另外发送环回那一份是为了保证同一台机器上多开实例时一定能互相发现。2026-09-26 实测 Windows 上两份都会到达，因此报文里带一个进程标识（系统时刻与进程号合成），由接收方把同一台主机的两份广播合并成一个房间。房间在三秒收不到新广播之后从列表里移除，因此主机崩溃时不会留下已经结束的房间。

**探测不到时的备用路径是手动填地址。** 跨机器能否收到广播取决于防火墙是否放行该 UDP 端口，这与游戏端口需要放行是同一件事，程序内无法解决。因此界面上始终保留地址与端口的输入框，主机的 HUD 上则显示本机的局域网地址，便于口头告知对方。

---

## 五、执行路线建议

1. **第 1 阶段 · 立项合规**：完成官方商标检索与域名注册，确定原创美术方向
2. **第 2 阶段 · 核心验证**：做出 1 个 2D 关卡的可玩原型，验证双元素协同手感
3. **第 3 阶段 · 系统搭建**：元素反应系统、AI 同伴、基础战斗
4. **第 4 阶段 · 内容填充**：PVE 副本、成长线、BOSS 战

---

## 六、TODO 清单

### 6.1 角色与美术

- [ ] 确定角色英文名方案：Molten & Frost 或拼音 Rong & Shuang
- [ ] 完成「熔」「霜」三视图与剪影测试（确保侧影可区分）
- [ ] 确认造型材质方向：熔岩结晶体 / 冰霜能量体
- [ ] 建立美术分层源文件归档规范

### 6.2 玩法与开发

- [x] 搭建联机连接层与服务端骨架（ENet，`--headless` 专用服务端，按连接的 peer 生成角色）
- [x] 把工程从 3D 改为 2D 横版（角色、场景、物理后端与输入映射；联机层与远端平滑只改向量类型）
- [ ] 搭建 1 个 2D 关卡可玩原型，验证双元素协同手感
- [ ] 实现元素反应系统（含熔 + 霜专属反应）
- [ ] 实现 AI 同伴与指令系统
- [ ] 搭建元素护盾怪物与破盾机制
- [ ] 设计技能树与装备成长线
- [ ] 设计首个多阶段元素机制 BOSS
- [ ] 在 Windows 与 macOS 上各跑通一次导出与自检（macOS 产 zip、Windows 产 exe），确认两台机器都能开发
